{
    "scripts": {
        "setup": "uv sync --all-extras",
        "run": "uv run pytest",
        "archive": "cd $CONDUCTOR_ROOT_PATH && (git merge --abort >/dev/null 2>&1 || true) && ((git diff --quiet && git diff --cached --quiet) || git stash push -u -m \"conductor-archive\") && git fetch origin && git merge origin/main"
    }
}
